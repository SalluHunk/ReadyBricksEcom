package presenter;

public interface AddDriverPresenter {
    void addDriver(String transportId,String firstName,String lastName,String phone,String password,String licenseImage,String driverPhoto);
}
